version: "3.4"
services:
  frontend:
    image: node:alpine
    container_name: check-your-insect-frontend
    ports:
      - 8080:8080
    working_dir: /srv/app
    volumes:
      - type: bind
        source: ./
        target: /srv/app
    command: sh -c "npm i && npm run dev"
    tty: true
